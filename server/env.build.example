# Build Metadata Environment Variables
# Copy this to .env or set these variables in your build process

# Application metadata
APP_NAME=viRtual POS
APP_VERSION=1.0.0

# Build metadata (set these during build process)
BUILD_SHA=abc123def456
BUILD_TIME=2024-01-15T10:30:00Z

# Example for CI/CD pipeline:
# BUILD_SHA=${GITHUB_SHA:-$(git rev-parse HEAD)}
# BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)

